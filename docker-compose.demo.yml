services:
  demo:
    build:
      context: .
      target: demo
    ports:
      - "8080:8080"
    environment:
      ORG_COST_DEMO: "1"
      CONFIG_PATH: /config/demo.yaml
      STATIC_DIR: /app/static
    volumes:
      - demo-history:/data/history

volumes:
  demo-history:
