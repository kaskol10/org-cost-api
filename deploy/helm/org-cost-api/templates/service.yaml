apiVersion: v1
kind: Service
metadata:
  name: {{ include "org-cost-api.fullname" . }}
  labels:
    {{- include "org-cost-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "org-cost-api.selectorLabels" . | nindent 4 }}
