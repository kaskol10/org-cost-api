# Screenshots

Capture from **demo mode** for README and docs:

```bash
# Terminal 1 — API + UI
docker compose -f docker-compose.demo.yml up --build

# Terminal 2 — dev frontend (optional, if not using bundled static)
cd frontend && npm run dev
```

Suggested captures (save as PNG here):

| File | View |
|------|------|
| `dashboard.png` | Main dashboard with org totals and top services |
| `trends.png` | Service trends / explain panel |
| `ask.png` | Ask tab with a sample question |

Use your OS screenshot tool or browser devtools. Commit PNGs when updating the README hero section.
